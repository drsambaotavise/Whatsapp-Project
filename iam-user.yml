# create user and add to group
# attach permissions to group
# add policies
# change to root user
# Make sure follow the Granular model
# orlando wants to create IAM policy to get objects
# Use of policy generator
# reference aws documentation on permission
# create role